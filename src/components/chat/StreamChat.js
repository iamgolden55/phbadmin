import React from 'react';

// Temporary placeholder for Stream Chat component
// This component has been disabled to resolve import errors
const StreamChatComponent = () => {
  return (
    <div className="stream-chat-placeholder">
      <div className="alert alert-info">
        <h5>🚧 Chat System Temporarily Disabled</h5>
        <p>The Stream Chat integration has been temporarily disabled while we focus on the Hospital Management dashboard.</p>
        <p>Chat functionality will be restored in a future update.</p>
      </div>
    </div>
  );
};

export default StreamChatComponent;